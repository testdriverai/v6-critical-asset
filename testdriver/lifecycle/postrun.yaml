version: 6.0.0
session: 67f00511acbd9ccac373edf7
steps:
  - prompt: send dashcam recording to server
    commands:
      - command: exec
        lang: pwsh
        code: dashcam -t 's&s ${TD_THIS_FILE}' -p # optional add -k SOMEID for the id of a folder in your Projects page at app.testdriver.ai
